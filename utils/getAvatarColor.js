export default letter => {
    const charCode = letter.charCodeAt();
    // if (charCode >= 1040 && charCode <= 1047) {
        return {
            background: "#2ecc71",
            color: "#ffffff",
        }
    // }
}